<%= simple_form_for([@article, Comment.new]) do |f| %>
  <%= f.input :name, label: "Nom et Prénom" %>
  <%= f.input :email, label: "Adresse email", hint: "Adresse strictement confidentielle" %>
  <%= f.input :comment, as: :text, label: "Votre commentaire" %>
  <%= f.submit "PUBLIER", class:"btn btn-primary carousel-btn" %>
<% end %>
